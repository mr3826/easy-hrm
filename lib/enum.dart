enum LeaveStatus { approved, pending, rejected, taken, cancelled ,reject,cancel}

enum EmploymentStatus { active, inactive, invited }

enum StartOrEndTimer { start, end }

enum Language { english, norwegian }


enum CandidateActivitiesLogsEnum { added_candidate_rating, applied_to_job,changed_candidate_name ,changed_hiring_stage}

